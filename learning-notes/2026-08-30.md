# Learning Notes — 2026-08-30

## What I learned

- A pull request compares a working branch with a base branch such as `main`.
- A branch needs at least one unique commit before GitHub can open a pull request for it.
- Additional commits pushed to the same branch automatically appear in its open pull request.

## Commands I practiced

```bash
git switch -c learning/2026-08-30-scheduler
git status
git add .
git commit -m "Add learning notes for first pull request"
git push -u origin learning/2026-08-30-scheduler
```

## Project progress

- Documented the planned concurrent task scheduler stages.
- Started CPU-bound prime-number tasks using trial division and sieve approaches.
- Prepared a branch-based workflow for reviewing work through pull requests.

## Next step

Compile and test the prime-number task, then decide how it will be represented in the task queue.
